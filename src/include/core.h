#ifndef TCCL_CORE_H_
#define TCCL_CORE_H_

#include <stdint.h>
#include <stdlib.h>

#include "debug.h"
#include "tccl.h"

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#endif  // end include guard
